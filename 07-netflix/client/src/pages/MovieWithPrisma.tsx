//import { PrismaClient } from "@pri"
//const prisma = new PrismaClient()


export default function MovieWithPrisma() {

  // Direct database access in a server component
  // const users = await prisma.movie.findMany({
  //   select: {
  //     id: true,
  //     name: true,
  //     email: true,
  //   },
  // })

  return(
    <div>
      movie with prisma pio boia
    </div>
  )
  
}